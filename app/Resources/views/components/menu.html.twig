<div class="list-group">
{% if categories is defined %}
        {% for category in categories %}
                <a href="{{ path("category_detail", {"id": category.id }) }}" class="list-group-item">{{ category.title }}</a>
        {% endfor %}
{% endif %}
</div>
